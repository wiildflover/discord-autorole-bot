# Discord Bot Configuration
# Author: Wildflover
# Description: Environment variables template for bot configuration

NODE_ENV=production
DISCORD_TOKEN=your_bot_token_here

# OAuth Configuration for Application Verification
# Note: OAUTH_REDIRECT_URI defaults to http://localhost:1420/auth/callback
# Only set this if you need a different redirect URI
DISCORD_CLIENT_ID=your_oauth_client_id_here
OAUTH_REDIRECT_URI=http://localhost:1420/auth/callback
